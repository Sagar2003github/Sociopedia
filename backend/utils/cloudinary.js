// Remove this entire file if you're not using Cloudinary anymore.
// This is how the file looks before removing.

import dotenv from "dotenv";
dotenv.config({});

// You don't need to export anything for Cloudinary anymore.
